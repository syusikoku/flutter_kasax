import 'package:flutter/material.dart';

/// 空的布局，啥都不做
class PlaceHolderUI extends StatelessWidget {
  const PlaceHolderUI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
